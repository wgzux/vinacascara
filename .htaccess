<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Prefix all requests with /public/ if not already present
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Security
Options -Indexes
AddDefaultCharset UTF-8
